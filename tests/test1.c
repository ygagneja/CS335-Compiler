void func(char* x, int y, ...);

int main(){
    // int x = printf("a");
    bool axx = true;
    char* ab;
    unsigned int i = 0;
    int a;
    int c = a;
    int x = c & a; // one line comment
    func("abc", x, c);
    for (i=a; i<c; i++){
        if (true);
            // printf ("hello");
    }
    while (a--){
        break;
    }
    x += a - c & i; 
    /* multi 
        line comment */
    return 0;
}